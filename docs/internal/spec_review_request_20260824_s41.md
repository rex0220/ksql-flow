# 仕様レビュー依頼: §4.1「途中のジョブが失敗したときのふるまい（まとめ）」の新設（担当: Codex）

**対象**: `docs/ksql_flow_spec.md` の §4.1（直近コミットの変更分。`git show HEAD -- docs/ksql_flow_spec.md` 等で差分確認可。見つからない場合は §4.1 全文をレビュー対象とする）
**性質**: 既存挙動の「要約 + 明文化」であり、新しい設計の提案ではない。5 か所（§3.2 / §5.1 / §6.3 / §7.2 / §7.3 / §8.2 / §10.3）に散在していた run-all 失敗時挙動の集約表 + 実装済みだが未文書だった 2 点（`maxApiCalls` 超過時の以降全ジョブ `SKIPPED（stop-on-error）` 停止・`batchTimeoutSec` 超過時の `SKIPPED（batch-timeout）`）の明文化。

**レビュー観点（この 3 つに限定。裁定済み設計の再提案は不要）**:

1. **既存章との矛盾**: §4.1 の表・箇条書きが、正である各節（特に §10.3 の終了コード集約 3 項・§8.2 の `SKIPPED (<理由>)` 前方一致契約・§6.3 の resume 選抜規則）と食い違っていないか。矛盾があれば §4.1 側を直す前提で指摘すること（§4.1 は要約であり正は各節）。
2. **実装との一致**: `src/commands/runAll.ts` の実挙動（`stopRequested` の遷移・`aggregateExit` / `aggregateStatus`・skip reason 文字列 `"stop-on-error"` / `"batch-timeout"` / `"dependency: x"` / `"filtered"` / `"LOCKED"`）と §4.1 の記述が一致するか。特に: `--continue-on-error` 時の依存スキップ理由、batch-timeout 時の BATCH ステータス、ApiLimitError 後の残ジョブの扱い。
3. **表の網羅性**: 「途中のジョブが失敗した」ときに読者が知りたい行動（続くのか・止まるのか・何が記録されるのか・Exit は何か・どう復旧するか）に穴がないか。ただし追加は最小限（この節は要約であり詳細の再掲ではない）。

**蒸し返し防止**: 既定続行 / `--stop-on-error` / `--continue-on-error` の 3 モード設計自体は裁定済み。レビュー対象は「§4.1 が既存仕様・実装を正しく要約しているか」だけ。

**出力**: `docs/internal/reviews/spec_review_codex-20260824-s41.md` に、指摘を重大度順で:

```
## 指摘 N: <一行要約>
- 重大度: BLOCKER（矛盾）/ MAJOR（誤解を招く）/ MINOR（曖昧・軽微）
- 該当箇所: §4.1 の該当行（および矛盾相手の §a.b または src の該当行）
- 内容: <欠陥の説明>
- 提案: <最小修正案>
```

指摘 0 件なら「異常なし」の 1 行でよい。レビュー結果ファイルの作成以外の変更（仕様書本体・ソースの編集）は行わないこと。
