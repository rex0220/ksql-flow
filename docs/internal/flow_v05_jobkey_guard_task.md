# v0.5 実装依頼 — `job_key` の入力ガード（担当: Codex / レビュー: Claude Code）

* 起票: 2026-08-28 ／ 根拠: `reviews/lock_design_debate-20260828.md`（全 6 ラウンドで唯一の確定コード欠陥と裁定）
* 現状の欠陥（コード確認済み）: `jobs.ts` はジョブ名を `meta.name ?? ファイル名` で**無検査**採用し、キーは素の連結（`runAll.ts:179` / `executor` 経由 `{profile}:{name}`）。
  1. ジョブ名 `__batch__` はバッチキーと恒久衝突 → 毎回 `SKIPPED (LOCKED)`（**命名ミスがロック競合に見える誤解を招く壊れ方**）
  2. `:` を含む profile / ジョブ名で別の (profile, job) 組と同一キーになり得る
  3. 完成キーの 64 文字検査なし（重複禁止フィールドの上限 — **上限値は kintone 実機で要確認**）

## 要件

1. **共通の不変条件として実装する**（`buildJobKey()` 相当の単一箇所）。**validate 専用にしない** — `validate` / `run` / `run-all` のすべてで、違反は設定ファイル読取後・API 呼び出し前に **Exit 1**
2. 検査内容:
   * ジョブ名の予約語: `__batch__` を拒否
   * profile 名・ジョブ名に `:` を含む場合は拒否
   * 完成キー `{profile}:{name}` が上限文字数以下（上限は実測で確定させ、コード内定数にコメントで出典を残す）
3. **文字数の数え方を kintone と一致させる**: JS の `.length`（UTF-16 コード単位）と kintone の「文字数」がサロゲートペア・絵文字で一致するかを**境界テストで確認**し、一致する数え方（必要ならコードポイント数）を採用
4. エラーメッセージは原因と対処を明示（例: 「ジョブ名 "__batch__" は予約語です。@ksql name で別名を指定してください」）
5. **キー形式は変更しない**（既存 `{profile}:{name}` の維持・入力制限のみ）。形式変更は新旧混在期間の二重実行問題を生むため採らない（討論で合意済み）
6. **CHANGELOG に移行案内を記載**: v0.4 まで動いていた名前が v0.5 で Exit 1 になり得る（`:` 入り・長すぎる名前）。該当時の対処（`@ksql name` での改名）を書く
7. テスト: 予約語・`:`・境界長（上限ちょうど / +1）・サロゲートペア境界・3 コマンドすべてで Exit 1、を網羅

## スコープ外（凍結済み — 再提案しない）

心拍化・終了時自己検査・400 分類変更・センチネル解放（`reviews/lock_design_debate-20260828.md` 第 5〜6 ラウンドの裁定。理由つきバックログとして凍結）。
