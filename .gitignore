# dependencies / build
node_modules/
dist/
*.tsbuildinfo

# ランナーの実行時生成物（ロック・state・ローカル JSONL ログ）
.ksql/

# ローカル運用ファイル（トークン等の env: 参照を含むため念のため除外。例は examples/ を参照）
ksql.config.json
.env
.env.*

# test / coverage
coverage/

# OS / editor
.DS_Store
Thumbs.db

# 実機検証の接続情報（実ドメイン・API トークン。リポジトリ外 %USERPROFILE%\.ksql-flow-dev\ が正位置）
docs/test-app/
api.txt
*.env
