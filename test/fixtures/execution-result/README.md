# Execution Result v1 fixtures

The three JSON fixtures in this directory are copied from kSQL-FlowNet commit
`c95cbc4`:

`C:/Users/rex02/Projects/ksql-flownet/tests/fixtures/execution-result/`

They are compatibility inputs for the canonical schema distributed by this
repository at `schema/execution-result-v1.schema.json`. Keep their JSON payloads
unchanged so contract drift is visible in tests.
